{
	"plugin": "env-minimal",
	"contractVersion": 1,
	"title": "Minimal Environment",
	"type": "component",
	"entry": "dist/index.mjs",
	"slot": "workspace-surface",
	"icon": "layers",
	"description": "A pinned environment card for a workspace corner post (doc 56 §3.2): checkout identity, this session's SCM ledger, and its task roll-up — all from the host props the shell already publishes, with react as the entire import surface. Pin it with widgets.pinned. It deliberately shows no live repository scan: that rides the workspace capability set, not a zero-import widget."
}
