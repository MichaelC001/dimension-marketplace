{
	"name": "@dimension/env-minimal",
	"author": "Dimension",
	"version": "0.1.0",
	"description": "Minimal Environment - a pinned environment card at the zero-import floor (doc 56 §3.2), bound by widgets.pinned. Import surface: react only; facts from the host props the shell publishes.",
	"type": "module",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"private": true,
	"scripts": {
		"build": "vite build"
	},
	"peerDependencies": {
		"react": ">=19"
	},
	"keywords": [
		"component",
		"widget",
		"environment",
		"store",
		"assembly"
	],
	"omp": {
		"name": "env-minimal",
		"description": "Minimal Environment - a pinned environment card at the zero-import floor (doc 56 §3.2), bound by widgets.pinned. Import surface: react only; facts from the host props the shell publishes.",
		"defaultEnabled": true,
		"category": "component",
		"keywords": [
			"component",
			"widget",
			"store"
		]
	},
	"devDependencies": {
		"vite": "8.1.5"
	}
}
