{
	"plugin": "classic-environment",
	"contractVersion": 1,
	"title": "Classic Environment",
	"type": "component",
	"entry": "dist/index.mjs",
	"slot": "workspace-surface",
	"icon": "layers",
	"description": "the environment card exactly as Dimension ships it, delivered as a marketplace component. It re-exports the shipped implementation from @fraym/ui (a granted pack external) rather than reimplementing it, so a space that binds this pack is pixel- and behaviour-identical to the built-in — assembled, not approximated."
}
