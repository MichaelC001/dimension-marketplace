// Three Lane is a LAYOUT plugin: its whole contribution is DATA in
// `dimension.plugin.json` — the slots it declares and the Demo Lane space
// that assembles on them (doc 68 §4.1, doc 69 §3). There is deliberately no
// runtime here: a layout is topology, ZERO pixels, and it ships no code.
export {};
