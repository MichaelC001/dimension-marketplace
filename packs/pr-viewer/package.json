{
	"name": "@dimension/pr-viewer",
	"author": "Dimension",
	"version": "0.1.2",
	"description": "PR Viewer — a dock component for the version-control lane (doc 73): linked reviews, stacks, detail with threads and diff, link/unlink, merge and rebase-stack. Contributed through the component lane; import surface: react + @fraym/ui.",
	"type": "module",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"private": true,
	"files": ["dimension.plugin.json", "dist", "README.md"],
	"scripts": {
		"build": "vite build",
		"typecheck": "tsc -p tsconfig.json --noEmit",
		"test": "bun test"
	},
	"peerDependencies": {
		"react": ">=19"
	},
	"keywords": ["component", "dock", "reviews", "pull-request", "store", "assembly"],
	"dimension": {
		"name": "pr-viewer",
		"description": "PR Viewer — reviews in the dock, from the Store's review keys. Installable and removable like any pack: with it a rail chip opens the review here, without it the browser.",
		"defaultEnabled": true,
		"category": "component",
		"keywords": ["component", "dock", "reviews", "pull-request", "store"]
	},
	"devDependencies": {
		"@fraym/ui": "workspace:*",
		"vite": "8.1.5"
	}
}
