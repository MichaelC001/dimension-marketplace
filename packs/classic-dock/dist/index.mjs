import { DOCK_CLASSIC } from "@fraym/ui";
//#region src/index.ts
/** The bundle contract (doc 68 §16.2): the host takes the DEFAULT export. */
var src_default = DOCK_CLASSIC.component;
//#endregion
export { src_default as default, DOCK_CLASSIC as implementation };
