{
	"name": "@dimension/classic-dock",
	"author": "Dimension",
	"version": "0.1.0",
	"description": "Classic Dock — the dock COLUMN — the tab strip, the resize handle, the panel body, as a marketplace pack.",
	"type": "module",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"private": true,
	"scripts": {
		"build": "vite build"
	},
	"peerDependencies": {
		"react": ">=19"
	},
	"devDependencies": {
		"vite": "8.1.5"
	},
	"omp": {
		"name": "classic-dock",
		"description": "Classic Dock - the shipped dock column (tab strip, resize handle, panel body), installable.",
		"defaultEnabled": true,
		"category": "component",
		"keywords": [
			"component",
			"dock",
			"classic"
		]
	}
}
