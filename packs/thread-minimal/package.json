{
	"name": "@dimension/thread-minimal",
	"author": "Dimension",
	"version": "0.1.0",
	"description": "Minimal Thread - a compact transcript for the session surface's thread section (doc 69 §3.5), bound by workspace.sections.thread. Import surface: react only; facts from the Store's published session/<id>/transcript + verb cells.",
	"type": "module",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"private": true,
	"scripts": {
		"build": "vite build"
	},
	"peerDependencies": {
		"react": ">=19"
	},
	"keywords": [
		"component",
		"thread",
		"section",
		"store",
		"assembly"
	],
	"omp": {
		"name": "thread-minimal",
		"description": "Minimal Thread - a compact transcript replacing the classic thread section by space binding. The first contributed thread section.",
		"defaultEnabled": true,
		"category": "component",
		"keywords": [
			"component",
			"thread",
			"store"
		]
	},
	"devDependencies": {
		"vite": "8.1.5"
	}
}
