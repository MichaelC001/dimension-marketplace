{
	"plugin": "thread-minimal",
	"contractVersion": 1,
	"title": "Minimal Thread",
	"type": "component",
	"entry": "dist/index.mjs",
	"slot": "thread",
	"icon": "messageSquare",
	"description": "A compact transcript for the session surface's thread section: role + text rows from the Store's published session cells, the working verb at the tail. The first contributed thread section, at the zero-import floor: react only, facts from the Store, pixels its own. Bind it with workspace.sections.thread."
}
