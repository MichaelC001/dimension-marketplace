import { HOST_SPLITS } from "@fraym/ui";
//#region src/index.ts
var implementation = HOST_SPLITS;
/** The bundle contract (doc 68 §16.2): the host takes the default export. */
var src_default = HOST_SPLITS.component;
//#endregion
export { src_default as default, implementation };
