{
	"name": "@dimension/composer-minimal",
	"author": "Dimension",
	"version": "0.1.0",
	"description": "Minimal Composer - the session surface's composer section at the zero-import floor (doc 69 §3.5), bound by workspace.sections.composer. Import surface: react only; reads the published session/<id>/isStreaming cell and sends through act(\"sendMessage\") / act(\"stopSession\").",
	"type": "module",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"private": true,
	"scripts": {
		"build": "vite build"
	},
	"peerDependencies": {
		"react": ">=19"
	},
	"keywords": [
		"component",
		"composer",
		"section",
		"store",
		"assembly"
	],
	"omp": {
		"name": "composer-minimal",
		"description": "Minimal Composer - replaces the classic composer section by space binding. The first contributed composer section.",
		"defaultEnabled": true,
		"category": "component",
		"keywords": [
			"component",
			"composer",
			"store"
		]
	},
	"devDependencies": {
		"vite": "8.1.5"
	}
}
