{
	"name": "@dimension/classic-thread",
	"author": "Dimension",
	"version": "0.1.0",
	"description": "Classic Thread - the session surface's thread section as Dimension ships it, delivered as a marketplace component (re-exports the shipped implementation from @fraym/ui; no reimplementation).",
	"type": "module",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"private": true,
	"scripts": {
		"build": "vite build"
	},
	"peerDependencies": {
		"react": ">=19"
	},
	"keywords": [
		"component",
		"thread",
		"classic",
		"assembly"
	],
	"omp": {
		"name": "classic-thread",
		"description": "Classic Thread - the shipped the session surface's thread section, installable.",
		"defaultEnabled": true,
		"category": "component",
		"keywords": [
			"component",
			"thread",
			"classic"
		]
	},
	"devDependencies": {
		"vite": "8.1.5"
	}
}
